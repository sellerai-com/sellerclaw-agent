# Admin UI (Vite build). Only variables prefixed with VITE_ are exposed to the client.
# Set at build time (e.g. empty when UI and API are same origin behind a reverse proxy).

# Base URL of the agent HTTP API. Leave empty if same origin as the UI.
VITE_AGENT_BASE_URL=

# Admin UI (Vite). Only variables prefixed with VITE_ are exposed to the client.
# See docs/developer/admin-ui.md

# Base URL of the agent HTTP API (CORS; docker compose maps agent to localhost:8001).
VITE_AGENT_BASE_URL=http://localhost:8001
